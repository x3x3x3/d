# The π-Sequence: A Self-Referential Recursion Encoding Prime Distribution

**项目简介**：本项目探讨一个由素数计数函数 `π(n)` 完全决定的自相似递归数列 `T_n`。该数列以一种非平凡的方式“编码”了所有素数信息，其闭合形式解已被推导。我们将其作为一个开放的数学对象发布，旨在邀请社区共同研究其渐近行为、波动模式及可能的数论意义。

### **1. 序列定义 (Definition)**

序列 `{T_n}` 由以下递归关系定义：
